import React from 'react';
import ShowListEvent from './ShowListEvent';

const StartButton = () => (
  <button type="button" className="button" onClick={ShowListEvent}>
    start
  </button>
);

export default StartButton;
